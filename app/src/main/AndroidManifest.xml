<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keecoding.newsapp">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/newsappicon"
        android:label="@string/app_name"
        android:name="com.keecoding.newsapp.presentation.NewsApp"
        android:roundIcon="@drawable/newsappicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewsAPIClient">
        <activity android:name="com.keecoding.newsapp.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>